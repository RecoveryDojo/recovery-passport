
The user is reporting `/peer-browser` returns 404. Looking at App.tsx, the existing route is `/peers/browse` registered to `PeerBrowsePage`. I need to check what the "Browse Peer Specialists" button on `/card` actually links to, and reconcile.

Let me check CardPage and PeerBrowsePage.
